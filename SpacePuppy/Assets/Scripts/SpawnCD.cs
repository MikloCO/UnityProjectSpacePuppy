using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SpawnCD : MonoBehaviour {

    /*
     * public GameObject[] asteroids;
     * 
     * void Start() {
     * 
     *  float randomCD = Random.Range(0F, 100F);
            if (randomCD < chanceToSpawn) {
                Spawn();
            }
            else {
                Destroy(gameObject);
            } 
     * private void SpawnCD () {
        Instantiate(CD[Random.Range(0, asteroids.Length)], transform); **Spawna bara CD inanför asteroid bälterna**
    }
     * }
     * 
     */
}
